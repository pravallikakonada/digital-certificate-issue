# Environment variables for production deployment
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=*

# Email settings for production
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=pravallikakonada984@gmail.com
EMAIL_HOST_PASSWORD=placeholder
DEFAULT_FROM_EMAIL=pravallikakonada984@gmail.com

# Database (SQLite for simplicity, can be upgraded to PostgreSQL)
# DATABASE_URL will be set by Render automatically